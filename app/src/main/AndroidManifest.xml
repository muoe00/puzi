<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.puzi.puzi">

  <uses-permission android:name="android.permission.INTERNET"/>

  <application
    android:name=".PuziApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <activity
      android:name=".ui.IntroActivity"
      android:label="@string/app_name"
      android:theme="@style/SplashTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".ui.MainActivity"
      android:theme="@android:style/Theme.Holo.Light.NoActionBar"
      android:windowSoftInputMode="adjustPan"/>

    <activity
      android:name=".ui.channel.ChannelDetailActivity"
      android:windowSoftInputMode="stateAlwaysHidden"/>
    <activity android:name=".ui.user.LevelActivity"></activity>
    <activity android:name=".ui.user.PointActivity"></activity>
    <activity android:name=".ui.user.RecommendActivity"></activity>
    <activity android:name=".ui.setting.SettingDetailActivity"></activity>
    <activity android:name=".ui.advertisement.AdvertisementDetailActivity">
      android:windowSoftInputMode="stateAlwaysHidden"
    </activity>
    <activity android:name=".ui.store.withdraw.WithdrawActivity"></activity>
    <activity android:name=".ui.store.coupon.CouponActivity"></activity>
    <activity android:name=".ui.channel.editorspage.EditorsPageActivity"/>
    <activity android:name=".ui.channel.ChannelFilterActivity"/>

    <activity
      android:name=".ui.company.CompanyDialog"
      android:theme="@style/Theme.AppCompat.Dialog"/>
  </application>

</manifest>